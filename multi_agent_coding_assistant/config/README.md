# Config

Centralized configuration loading and validation.

This project uses:
- `python-dotenv` to load environment variables from `.env`
- `pydantic-settings` (via `config/settings.py`) for typed settings

